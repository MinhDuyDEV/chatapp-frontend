/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "dfig1ic46fo87.cloudfront.net",
      },
    ],
  },
};

export default nextConfig;
